services:
  lb:
    image: haproxy:alpine
    cpuset: "0"
    volumes:
      - ./loadbalancers/haproxy/haproxy-unix.cfg:/usr/local/etc/haproxy/haproxy.cfg:ro
      - sockets:/sockets
    ports:
      - "9999:9999"
    depends_on:
      api1:
        condition: service_started
      api2:
        condition: service_started
    networks:
      - rinha
    deploy:
      resources:
        limits:
          cpus: "0.15"
          memory: "50MB"

  api1:
    image: ${IMAGE:-villanidev/rinha-de-backend-2026:latest}
    cpuset: "1"
    environment:
      SERVER_SOCKET_PATH: /sockets/api1.sock
      APP_VECTOR_SEARCH_INDEX: ivf_pq
      APP_VECTOR_SEARCH_NPROBE: "8"
      APP_VECTOR_SEARCH_CANDIDATES: "20"
      APP_VECTOR_SEARCH_RERANK_ENABLED: "true"
      APP_VECTOR_SEARCH_RERANK_NPROBE: "8"
      APP_VECTOR_SEARCH_RERANK_CANDIDATES: "20"
      APP_FRAUD_DEBUG: "false"
    volumes:
      - sockets:/sockets
    networks:
      - rinha
    deploy:
      resources:
        limits:
          cpus: "0.425"
          memory: "150MB"

  api2:
    image: ${IMAGE:-villanidev/rinha-de-backend-2026:latest}
    cpuset: "2"
    environment:
      SERVER_SOCKET_PATH: /sockets/api2.sock
      APP_VECTOR_SEARCH_INDEX: ivf_pq
      APP_VECTOR_SEARCH_NPROBE: "8"
      APP_VECTOR_SEARCH_CANDIDATES: "20"
      APP_VECTOR_SEARCH_RERANK_ENABLED: "true"
      APP_VECTOR_SEARCH_RERANK_NPROBE: "8"
      APP_VECTOR_SEARCH_RERANK_CANDIDATES: "20"
      APP_FRAUD_DEBUG: "false"
    volumes:
      - sockets:/sockets
    networks:
      - rinha
    deploy:
      resources:
        limits:
          cpus: "0.425"
          memory: "150MB"

volumes:
  sockets:

networks:
  rinha:
    driver: bridge
