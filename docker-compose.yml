services:
  lb:
    image: haproxy:alpine
    volumes:
      # - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./haproxy.cfg:/usr/local/etc/haproxy/haproxy.cfg:ro
    ports:
      - "9999:9999"
    depends_on:
      - api1
      - api2
    networks:
      - rinha
    deploy:
      resources:
        limits:
          cpus: "0.2"
          memory: "30MB"

  api1:
    image: villanidev/rinha-de-backend-2026:20260504025938
    environment:
      APP_VECTOR__SEARCH_INDEX: ivf_pq
      APP_VECTOR__SEARCH_NPROBE: "8"
      APP_VECTOR__SEARCH_NPROBE__GRAY: "4"
      APP_VECTOR__SEARCH_CANDIDATES: "10"
      APP_VECTOR__SEARCH_RERANK: "false"
      APP_FRAUD_DEBUG: "false"
    networks:
      - rinha
    deploy:
      resources:
        limits:
          cpus: "0.4"
          memory: "160MB"

  api2:
    image: villanidev/rinha-de-backend-2026:20260504025938
    environment:
      APP_VECTOR__SEARCH_INDEX: ivf_pq
      APP_VECTOR__SEARCH_NPROBE: "8"
      APP_VECTOR__SEARCH_NPROBE__GRAY: "4"
      APP_VECTOR__SEARCH_CANDIDATES: "10"
      APP_VECTOR__SEARCH_RERANK: "false"
      APP_FRAUD_DEBUG: "false"
    networks:
      - rinha
    deploy:
      resources:
        limits:
          cpus: "0.4"
          memory: "160MB"

networks:
  rinha:
    driver: bridge
